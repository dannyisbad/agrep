# build output
/target
/dist
/dist-test
*.bin

# the built index - your entire chat history lives here. NEVER commit this.
/data
/corpus.db

# benchmark datasets + their throwaway index homes (fetched, never committed)
/bench/.data
/bench/.home*
/bench/.mp-venv
/bench/.qhome-test
/bench/.work-ctxbench

# local-only: strategy notes + personal shell shims
/WHERE-AGREP-EATS.md
/agrep.ps1
/checklist.md

# python
/py/.venv
/.venv
/.pytest_cache
/uv.lock
__pycache__/
*.pyc

# local credentials and tool state
.env
.env.*
!.env.example
/.opencode

# local scratch/debug helper
py/_check.py

# tooling / scratch
/web/shots/
/.playwright-mcp
*.log
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*.db-journal

# downloaded model and numeric artifacts
*.onnx
*.safetensors
*.npy
*.npz
*.pt
*.pth

# private session dumps + capture output - real transcripts land here. NEVER commit these.
# (machine-specific one-off dump names live in .git/info/exclude so they never enter history)
/out_*
/err_*
/bench/render/
/ar.err
/err.corrupt
/err.stderr
/full_recall.txt
/j1.txt
/j2.txt
/ns.err
/out.stderr
/out.stdout
/p1.err
/p1.out
/p2.out
/p3.out
/rc.stderr

# local scratch
*.local.md
*.local.json

# OS cruft
.DS_Store
Thumbs.db

# prebuilt rust binary staged for wheel packaging (CI / local build)
_bin/
.codex/
