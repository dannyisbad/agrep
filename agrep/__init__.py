"""agrep - grep your cross-agent chat history, one command.

This package ships the flat agrep CLI and Python engines plus the prebuilt Rust ingest
binary, so `uvx agrep` / `pipx run agrep` / `pip install agrep` need no clone or Cargo.
The real logic lives in the bundled modules; see __main__.py.
"""

__version__ = "0.2.0"
